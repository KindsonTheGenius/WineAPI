# WineAPI
Wine API Task for BimObject Role

# Instructions
Clone the repository and open in Visual Studio
Simply run the application. The seeders are executed to load the initial data
Next, it launches the browser and opens the Swagger documentation

Database is implemented in SQL Lite, so no configurations is required.
